// this file is generated by the build process Thu Jun 06 2019 12:40:02 GMT+0800 (Philippine Standard Time) 

const mongoose = require('mongoose');
const fs = require('fs');
const path = require('path');
const types = require('./types');
const jsonSchema = require('./jsonSchema');
const configData = fs.readFileSync(path.resolve(__dirname, '../../lib/config/config.json'));
const config = JSON.parse(configData);
mongoose.connect(config.mongodbConnection);

const model = {};

model.cinemauser = require('./models/cinemauser');
model.eplususer = require('./models/eplususer');
model.ticketuser = require('./models/ticketuser');
model.userschema = require('./models/userschema');


exports.model = model;
exports.fileSystem = fs;
exports.types = types;
exports.mongoose = mongoose;
exports.jsonSchema = jsonSchema;
